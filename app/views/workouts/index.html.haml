%h2= t('.title')
%table.table.table-bordered.table-hover
  %thread
    %tr
      %th= t('.date')
      %th= t('.workout_type')
      %th= t('.duration')
      %th= t('.calories')
      %th


  %tbody
    - @workouts.reverse_each do |workout|
      %tr
        %td= workout.date
        %td= workout.workout_type
        %td= workout.duration
        %td= workout.calories
        %td= link_to t('.details'), workout
%br

= link_to t('.new'), new_workout_path
